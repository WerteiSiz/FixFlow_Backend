#  FixFlow  
## Система управления дефектами на строительных объектах  
**Frontend: Python + Flet**  
**Backend: FastAPI + API Gateway + Microservices**

---

## Описание проекта

**FixFlow** — это полнофункциональная платформа для контроля и управления дефектами на строительных объектах.  
Цель решения — создать удобную и прозрачную систему для инженеров, менеджеров и руководителей, где каждый может эффективно решать свои задачи:

- инженеры фиксируют дефекты;  
- менеджеры контролируют сроки и распределяют задачи;  
- руководство получает аналитику и отчеты.

Backend реализован на **микросервисной архитектуре** с FastAPI, API Gateway и сервисами пользователей и дефектов.  
Frontend построен на **React** и работает в браузере, на десктопе и мобильных устройствах.

---

#  Архитектура проекта

```
┌────────────────┐        ┌──────────────────┐
│  FixFlow Frontend │ <───> │  API Gateway (8000) │
└────────────────┘        └──────────────────┘
                                   │
                   ┌───────────────┴───────────────┐
                   │                                 │
       ┌────────────────────┐            ┌────────────────────┐
       │ Users Service       │            │ Defects/Orders Svc │
       │  (Auth, Roles)      │            │ Управление задачами │
       │ Port: 8001          │            │ Port: 8002          │
       └────────────────────┘            └────────────────────┘
```

- **API Gateway** — маршрутизация, контроль прав, rate limiting, X-Request-ID  
- **Users Service** — аутентификация, роли, управление пользователями  
- **Orders/Defects Service** — создание и управление дефектами  
- **Frontend (Flet)** — удобный UI для всех ролей

---

# Возможности FixFlow

### Система ролей
- Инженер  
- Менеджер  
- Руководитель

### Управление дефектами
- Создание, назначение и обновление статуса  
- Добавление комментариев  
- История изменений  
- Загружаемые фото/медиа (при интеграции)

###  Управление объектами
- Проекты  
- Зоны ответственности  
- Привязка исполнителей  

### Аналитика и отчёты
- Диаграммы и графики эффективности  
- Статистика по объектам  
- Экспорт отчётов

###  Адаптивность
Работает на:
- Web  
- Desktop  

---

#  Установка и запуск

##  1. Backend (FastAPI + Docker Compose)

```bash
git clone <repo>
cd fastapi-microtasks
docker-compose up --build
```

Сервисы поднимутся на:

| Сервис | Порт | Описание |
|--------|------|----------|
| API Gateway | 8000 | Входная точка API |
| Users Service | 8001 | Пользователи, авторизация |
| Defects/Orders Service | 8002 | Управление дефектами |

---

## 2. Frontend (FixFlow)

Перейдите в папку фронта:

```bash
pip install -r requirements.txt
python main.py
```

После запуска откройте:

```
http://localhost:8080
```

---

#  Роли и права доступа

##  Инженер
- Создание дефектов  
- Изменение статусов  
- Добавление комментариев  
- Просмотр задач  

##  Менеджер
- Всё из роли Инженера  
- Назначение исполнителей  
- Контроль сроков  
- Управление объектами  
- Генерация отчётов  

##  Руководитель
- Полный доступ  
- Аналитика  
- Сводные отчёты  
- Управление всеми объектами  

---

#  Структура проекта Frontend (FixFlow)

```
FixFlow/
├── main.py                 
├── components/             
│   ├── card.py            
│   └── navigation.py      
├── pages/                 
│   ├── home_page.py       
│   ├── auth_page.py       
│   ├── dashboard_page.py  
│   ├── objects_page.py    
│   └── reports_page.py    
├── utils/                 
│   └── colors.py          
└── requirements.txt        
```

---

#  Цветовая палитра UI

| Цвет | HEX | Назначение |
|------|------|------------|
| Основной фон | `#F5F5F5` | Фон |
| Основной цвет | `#4A2C21` | Меню, акценты |
| Вторичный | `#CBC0B2` | Карточки, панели |
| Критический | `#550B14` | Ошибки, статус "Критично" |
| Приглушённый текст | `#7E6961` | Подписи, вторичный текст |

---

# Структура backend проекта (микросервисы)

```
fastapi-microtasks/
├── api_gateway/
│   ├── app/
│   └── Dockerfile
├── service_users/
│   ├── app/
│   └── Dockerfile
├── service_orders/
│   ├── app/
│   └── Dockerfile
├── docker-compose.yml
└── tests/
```

---

#  Основные API эндпоинты

##  Users Service

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/v1/auth/register` | Регистрация |
| POST | `/v1/auth/login` | Авторизация |
| GET | `/v1/users/me` | Профиль |
| PUT | `/v1/users/me` | Обновление профиля |
| GET | `/v1/users` | (Admin) Список пользователей |

## Defects/Orders Service

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/v1/orders` | Создание дефекта |
| GET | `/v1/orders/{id}` | Получение информации |
| GET | `/v1/orders` | Список дефектов |
| PATCH | `/v1/orders/{id}/status` | Изменение статуса |
| DELETE | `/v1/orders/{id}` | Отмена/закрытие |

---

#  Демо данные

FixFlow включает:
- Примеры объектов
- Примерные дефекты
- Графики по прогрессу
- Примеры отчетов

---

#  Технологии

### Backend
- FastAPI  
- SQLModel  
- Docker & Docker Compose  
- Async HTTPX  
- JWT авторизация  
- Rate limiting (SlowAPI)  
- X-Request-ID, логирование  

### Frontend
- React+Typescript
- Matplotlib  
- Адаптивный UI  

# 📝 Лицензия  
Проект разработан для **ООО «СистемаКонтроля»**  

---

# Быстрый старт для разработчика

1) Установите зависимости для локальной разработки (используется виртуальное окружение в `service_users/.venv` в примере):

```powershell
python -m venv service_users/.venv
service_users\.venv\Scripts\pip install -r service_users/app/requirements.txt
service_orders\.venv\Scripts\pip install -r service_orders/app/requirements.txt
api_gateway\.venv\Scripts\pip install -r api_gateway/app/requirements.txt
```

2) Как запустить тесты локально (в корне репозитория):

```powershell
service_users\.venv\Scripts\python -m pytest -q
```

3) OpenAPI спецификация: [docs/openapi.yaml](docs/openapi.yaml)

# Переменные окружения (обязательные/рекомендуемые)

- `JWT_SECRET` – секрет для подписи JWT (рекомендуется задавать в CI/секретах). Default: `CHANGE_ME_SECRET`.
- `USERS_URL` – URL сервиса пользователей (используется в `api_gateway`). Пример: `http://service_users:8001`.
- `ORDERS_URL` – URL сервиса заказов. Пример: `http://service_orders:8002`.
- `USERS_DB_FILE` – путь к sqlite-файлу пользователей (по умолчанию `data/users.db`).
- `ORDERS_DB_FILE` – путь к sqlite-файлу заказов (по умолчанию `data/orders.db`).
- `RATE_LIMIT` – лимит запросов для `api_gateway` (формат slowapi), пример: `100/minute`.
- `CORS_ORIGINS` – список origin'ов через запятую или `*` (по умолчанию `*`).
- `OTEL_COLLECTOR_URL` – адрес OTLP collector для экспорта трасс (опционально).

# Хеширование паролей и заметки по Docker

- `HASH_ALGORITHM` — выбор алгоритма хеширования паролей. Допустимые значения: `bcrypt` или `pbkdf2_sha256`. По умолчанию: `pbkdf2_sha256`.

Если вы установите `HASH_ALGORITHM=bcrypt`, убедитесь, что пакет `bcrypt` может быть собран/установлен в образе. Для Debian/образов на базе `python:*-slim` установите системные зависимости перед запуском `pip install` (в `service_users/Dockerfile` приведён пример):

```dockerfile
RUN apt-get update && apt-get install -y --no-install-recommends \
       build-essential libffi-dev libssl-dev \
 && rm -rf /var/lib/apt/lists/*
```

В качестве альтернативы можно использовать предсобранный wheel для `bcrypt` (многие CI-раннеры предоставляют такие колёса) либо оставить алгоритм по умолчанию `pbkdf2_sha256`, который не требует нативных зависимостей.

Пример (PowerShell):

```powershell
$env:JWT_SECRET = "supersecret"
$env:USERS_DB_FILE = "data/users.db"
$env:ORDERS_DB_FILE = "data/orders.db"
$env:OTEL_COLLECTOR_URL = "http://localhost:4318"
```

Запуск локального OpenTelemetry Collector

Для проверки экспорта трасс можно поднять локальный Collector через `docker-compose` — в проект уже добавлён сервис `otel-collector` и пример конфигурации `otel-collector-config.yaml`. Collector будет слушать OTLP HTTP на порту `4318` и логировать входящие трассы в своих логах.

Запуск всех сервисов с Collector:

```bash
docker-compose up --build
```

Просмотреть логи Collector (в которых появятся трассы):

```bash
docker-compose logs -f otel-collector
```

Если сервисы настроены с `OTEL_COLLECTOR_URL=http://otel-collector:4318`, они отправят трассы в Collector и вы увидите лог-записи трасс в выводе.

Дополнительно:

- `JWT_SECRET_FILE` — путь к файлу, содержащему секрет для подписи JWT. Этот способ удобен при использовании Docker secrets: файл с секретом монтируется в контейнер, а приложение читает его по пути и не хранит секреты в переменных окружения.

Пример использования Docker secrets в `docker-compose.yml` (сокращённо):

```yaml
version: '3.8'
services:
       service_users:
              image: fixflow_service_users:latest
              secrets:
                     - jwt_secret
              environment:
                     - JWT_SECRET_FILE=/run/secrets/jwt_secret
       api_gateway:
              image: fixflow_api_gateway:latest
              secrets:
                     - jwt_secret
              environment:
                     - JWT_SECRET_FILE=/run/secrets/jwt_secret
secrets:
       jwt_secret:
              file: ./secrets/jwt_secret.txt
```

Если вы не используете Docker secrets, можно передать значения через `.env`/env-file (менее безопасно):

```
JWT_SECRET=super-secret-from-env
```

и подключить файл в `docker-compose.yml` через `env_file: .env`.

